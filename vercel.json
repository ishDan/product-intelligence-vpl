{
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "framework": "vite",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/$1" }
  ],
  "functions": {
    "api/fetch-specs.js": { "maxDuration": 60 },
    "api/batch-check.js": { "maxDuration": 60 }
  }
}
